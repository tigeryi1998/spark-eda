# buildconfig-producer.yaml

apiVersion: image.openshift.io/v1
kind: ImageStream
metadata:
  name: spark-eda-producer
  labels:
    app: spark-eda-producer
spec:
  lookupPolicy:
    local: false
  tags:
    - name: latest
---
apiVersion: build.openshift.io/v1
kind: BuildConfig
metadata:
  name: spark-eda-producer
  labels:
    app: spark-eda-producer
spec:
  output:
    to:
      kind: ImageStreamTag
      name: spark-eda-producer:latest
  source:
    type: Git
    git:
      uri: https://github.com/tigeryi1998/spark-eda
    contextDir: .
    sourceSecret:
      name: ds551 # Replace with your actual secret name if needed
  strategy:
    type: Docker
    dockerStrategy:
      dockerfilePath: producer/Containerfile
  triggers:
    - type: ConfigChange
    - type: GitHub
      github:
        secret: spark-eda-producer-github-webhook-secret
    - type: Generic
      generic:
        secretReference:
          name: spark-eda-producer-generic-webhook-secret
  runPolicy: Serial
  successfulBuildsHistoryLimit: 5
  failedBuildsHistoryLimit: 5
